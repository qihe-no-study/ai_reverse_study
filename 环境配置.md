# 环境配置指南

## ⚡ 三步快速开始

```bash
# 1. Python 依赖
pip install -r requirements.txt

# 2. Node.js（fnm 切换）
fnm use 24   # 或 fnm use 22

# 3. 维普项目额外依赖（仅维普需要）
cd qikan/code && pnpm install
```

---

## 🐍 Python 环境

### 一键安装

```bash
pip install -r requirements.txt
```

### 依赖清单

| 包 | 版本 | 用途 |
|----|------|------|
| `requests` | >=2.28.0 | HTTP 请求（QQ音乐 API / 维普页面采集） |

### 手动安装（备选）

```bash
pip install requests>=2.28.0
```

### 使用虚拟环境（推荐）

```bash
conda create -n js_reverse python=3.11 -y
conda activate js_reverse
pip install -r requirements.txt
```

---

## 📗 Node.js 环境

### 安装 fnm（Node.js 版本管理器）

```bash
# Windows
winget install Schniz.fnm

# macOS / Linux
curl -fsSL https://fnm.vercel.app/install | bash
```

### 安装 Node.js

```bash
fnm install 24        # 维普项目推荐（sdenv 需要 >=20.19.5）
fnm install 22        # QQ音乐可用
fnm default 24
fnm use 24
```

### 各项目版本要求

| 项目 | 最低版本 | 推荐版本 | 原因 |
|------|---------|---------|------|
| QQ音乐 | v18.0.0 | v22 | JSVMP VM 执行，需独立作用域规避 Node v24 全局 navigator |
| 维普期刊 | v20.19.5 | v24 | sdenv v1.1.3 依赖 undici ^7 |

### 验证

```bash
node -v   # v24.x 或 v22.x
npm -v    # 10.x
```

---

## 📦 pnpm + sdenv（仅维普项目）

### 安装 pnpm

```bash
npm install -g pnpm
```

### 安装 VS 2022 Build Tools

sdenv 依赖 `canvas` C++ 模块，需要编译工具链：

1. 下载 [Visual Studio 2022 Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)
2. 安装时勾选 **「使用 C++ 的桌面开发」**
3. 配置 npm：

```powershell
# PowerShell（管理员）
npm config set msvs_version 2022
```

### 安装项目依赖

```bash
cd qikan/code
pnpm install
```

`.npmrc` 已预配置：

```ini
onlyBuiltDependencies:
  - canvas
  - sdenv
```

### 验证

```bash
node -e "require('sdenv'); console.log('sdenv OK')"
# 输出: sdenv OK
```

---

## 🌐 Chrome 调试环境（可选）

用于 MCP 浏览器自动化：

```bash
# 安装 chrome-devtools-mcp
npm install -g chrome-devtools-mcp

# 启动 Chrome 调试模式
"C:/Program Files/Google/Chrome/Application/chrome.exe" \
  --remote-debugging-port=9222 \
  --user-data-dir="%USERPROFILE%/.claude/chrome-debug-profile"
```

Claude Code 配置（`~/.claude/settings.json`）：

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "chrome-devtools-mcp",
      "args": ["--browserUrl", "http://127.0.0.1:9222"]
    }
  }
}
```

---

## 🛠️ Git & GitHub

```bash
# 生成 SSH Key（如果没有）
ssh-keygen -t ed25519 -C "your@email.com"

# 添加到 GitHub: https://github.com/settings/keys
cat ~/.ssh/id_ed25519.pub

# 验证
ssh -T git@github.com
# 输出: Hi <username>! You've successfully authenticated...
```

---

## ✅ 一键环境检查

```bash
#!/bin/bash
echo "=== 环境检查 ==="
python --version 2>&1 || echo "❌ Python 未安装"
node -v 2>&1 || echo "❌ Node.js 未安装"
npm -v 2>&1 || echo "❌ npm 未安装"
pnpm -v 2>&1 || echo "⚠️ pnpm 未安装（仅维普需要）"
git --version 2>&1 || echo "❌ Git 未安装"
ssh -T -o ConnectTimeout=3 git@github.com 2>&1 | head -1
node-gyp --version 2>&1 || echo "⚠️ node-gyp 未安装（仅维普需要）"
echo "=== 检查完毕 ==="
```

---

## 🔧 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `pnpm install` canvas 编译失败 | 缺少 VS Build Tools | 安装 VS2022 C++ 桌面开发 → `npm config set msvs_version 2022` |
| `fnm` 命令不存在 | 未加入 PATH | 重启终端 或 `eval "$(fnm env)"` |
| GitHub SSH `Permission denied` | 未配置 SSH Key | `ssh-keygen -t ed25519` → 添加到 GitHub Settings |
| `requests` 返回 412/403 | Cookie 过期 | QQ音乐: 用 access_token 刷新 musickey / 维普: 重跑 sdenv |
| sdenv 超时 12s | 网络不通或版本过低 | 检查 Node >= 20.19.5，确认可访问 qikan.cqvip.com |
| vis-network 下载失败 | CDN 不可达 | 从本仓库 understand-anything/ 目录复制已有文件 |
