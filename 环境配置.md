# 环境配置指南

本文档涵盖 `ai_reverse_study` 仓库所有项目的完整环境配置。

## 📊 环境总览

| 组件 | QQ音乐 | 维普期刊 | 说明 |
|------|--------|---------|------|
| Python | 3.x | 3.x | 请求调度 + 数据解析 |
| Node.js | v18+ | v20.19.5+ | JSVMP 虚拟机 / sdenv |
| pnpm | — | latest | 维普项目包管理 |
| npm | — | — | 全局工具安装 |
| Git | — | — | 版本管理 |
| VS Build Tools | — | 2022 | sdenv C++ 模块编译 |
| Chrome | — | — | 调试 / MCP 工具 |

---

## 🐍 Python 环境

### 安装

推荐使用 [Miniconda](https://docs.conda.io/en/latest/miniconda.html) 管理 Python 环境：

```bash
# 创建独立环境
conda create -n js_reverse python=3.11
conda activate js_reverse
```

### 依赖安装

```bash
# QQ音乐
pip install requests

# 维普期刊
pip install requests
```

### 验证

```bash
python -c "import requests; print('requests', requests.__version__)"
# 输出: requests 2.31.0
```

---

## 📗 Node.js 环境

### 安装

推荐使用 [fnm](https://github.com/Schniz/fnm)（跨平台 Node.js 版本管理器）：

```bash
# Windows (PowerShell)
winget install Schniz.fnm

# 安装 LTS 版本
fnm install 22
fnm use 22
fnm default 22

# 安装 v24（维普项目需要新特性）
fnm install 24
```

### 多版本切换

QQ音乐用 v18+ 即可，维普期刊需要 v20.19.5+：

```bash
# QQ音乐 — v18 足够
fnm use 18

# 维普期刊 — 需要 v20+
fnm use 24

# 查看当前版本
node -v
```

### 验证

```bash
node -v  # 应输出 v18.x 或更高
npm -v   # 应输出 9.x 或更高
```

---

## 📦 pnpm（维普项目）

### 安装

```bash
npm install -g pnpm
```

### 配置

维普项目需要编译 C++ 原生模块（canvas, sdenv），创建 `.npmrc`：

```ini
# 项目根目录 code/.npmrc
onlyBuiltDependencies:
  - canvas
  - sdenv
```

### 安装项目依赖

```bash
cd qikan/code
pnpm install
```

如果安装失败见下方 [常见问题](#-常见问题)。

---

## 🔧 VS 2022 Build Tools（维普项目）

sdenv 依赖 `canvas` 等 C++ 原生模块，需要编译工具链。

### 安装

1. 下载 [Visual Studio 2022 Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)
2. 运行安装程序，勾选 **「使用 C++ 的桌面开发」** 工作负荷
3. 安装完成后重启终端

### 验证

```bash
npm install -g node-gyp
node-gyp --version
# 应输出版本号
```

### 手动配置（如果自动检测失败）

```bash
# PowerShell (管理员)
npm config set msvs_version 2022
npm config set python python3
```

---

## 🌐 Chrome 调试环境

### Chrome DevTools Protocol

用于 MCP 浏览器自动化（创建仓库、GitHub Pages 配置等）：

```bash
# 启动带调试端口的 Chrome
"C:/Program Files/Google/Chrome/Application/chrome.exe" \
  --remote-debugging-port=9222 \
  --user-data-dir="C:/Users/<用户名>/.claude/chrome-debug-profile"
```

### chrome-devtools-mcp

```bash
npm install -g chrome-devtools-mcp
```

Claude Code 配置（`~/.claude/settings.json`）：

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "chrome-devtools-mcp",
      "args": ["--browserUrl", "http://127.0.0.1:9222"]
    }
  }
}
```

---

## 🛠️ 辅助工具

### Git & GitHub

```bash
# 验证 SSH 连接
ssh -T git@github.com
# 输出: Hi <username>! You've successfully authenticated...

# 配置用户信息
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
```

### 代理配置（按需）

```bash
# HTTP 代理（替换为你的代理地址）
set HTTP_PROXY=http://127.0.0.1:7890
set HTTPS_PROXY=http://127.0.0.1:7890

# 或使用 git 配置
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890
```

### vis-network 下载

知识图谱 skill 需要 vis-network：

```bash
# 自动（skill 内部处理）
curl -sL -o "vis-network.min.js" \
  "https://unpkg.com/vis-network@9.1.9/dist/vis-network.min.js"

# 国内镜像（如果 unpkg 不可用）
curl -sL -o "vis-network.min.js" \
  "https://ghproxy.com/https://raw.githubusercontent.com/visjs/vis-network/v9.1.9/dist/vis-network.min.js"
```

---

## ✅ 环境检查清单

运行以下命令确认所有环境就绪：

```bash
# 逐一检查
python --version      # Python 3.x
node -v               # Node.js v18+（维普需要 v20+）
npm -v                # npm
pnpm -v               # pnpm（维普）
git --version         # Git
ssh -T git@github.com # GitHub SSH
node-gyp --version    # node-gyp（维普）

# 一键检查脚本
cat << 'EOF' > /tmp/check_env.sh
echo "=== 环境检查 ==="
python --version 2>&1 || echo "❌ Python 未安装"
node -v 2>&1 || echo "❌ Node.js 未安装"
npm -v 2>&1 || echo "❌ npm 未安装"
pnpm -v 2>&1 || echo "⚠️ pnpm 未安装（仅维普需要）"
git --version 2>&1 || echo "❌ Git 未安装"
ssh -T git@github.com 2>&1 | head -1
node-gyp --version 2>&1 || echo "⚠️ node-gyp 未安装（仅维普需要）"
echo "=== 检查完毕 ==="
EOF
bash /tmp/check_env.sh
```

---

## 🔧 常见问题

### Q: `pnpm install` 报错 canvas 编译失败

```
A: 1. 确认已安装 VS 2022 Build Tools（含 C++ 桌面开发）
   2. 管理员 PowerShell 执行: npm config set msvs_version 2022
   3. 删除 node_modules 重试: rm -rf node_modules && pnpm install
```

### Q: Node.js 版本过低（`fnm` 未生效）

```
A: 在终端执行 eval "$(fnm env)" 或重启终端
   永久生效: 将 eval "$(fnm env)" 加到 ~/.bashrc
```

### Q: GitHub SSH 连接失败

```
A: 1. 确认已添加 SSH key: cat ~/.ssh/id_ed25519.pub
   2. 未生成则: ssh-keygen -t ed25519 -C "your@email.com"
   3. 添加到 GitHub: https://github.com/settings/keys
   4. 测试: ssh -T git@github.com
```

### Q: `requests` 请求返回 412/403

```
A: Cookie 过期或签名错误
   - QQ音乐: 刷新 musickey（用 access_token 调用 QQLogin）
   - 维普期刊: 重新运行 sdenv 生成新 Cookie
```

### Q: sdenv 超时（12s 无响应）

```
A: 1. 检查 Node.js 版本 >= 20.19.5
   2. 检查网络能否访问 qikan.cqvip.com
   3. 增加超时时间: 修改 sdenv_rs6.js 中的 setTimeout 参数
```

### Q: `vis-network` 下载失败

```
A: 1. 使用 ghproxy 镜像
   2. 手动下载放到 skill 的 references/ 目录
   3. 从本仓库的 understand-anything/ 目录复制已有文件
```
