# 维普期刊 — RS6 瑞数反爬 逆向流程说明

## 项目概况

| 项目 | 内容 |
|------|------|
| 目标页面 | `https://qikan.cqvip.com/Qikan/Journal/JournalGuid?from=index` |
| 防护机制 | 瑞数 RS6（412 JS 挑战 + $_ts VM + S+T Cookie 对） |
| 复杂度 | L3: 虚拟机保护 + 环境依赖 |
| 关键发现 | sdenv 自动处理完整 412 → Cookie → 200 流程 |
| 方案 | 路径 B 环境伪装（sdenv + jsdom），非路径 A 算法追踪 |

## 完成功能

| 功能 | 状态 | 说明 |
|------|------|------|
| Cookie 生成 | ✅ | sdenv 环境伪装 + VM 字节码执行 |
| 真实页面获取 | ✅ | HTTP 200 + 完整 HTML |
| 无浏览器自动化 | ✅ | jsdom + requests，符合红线要求 |

## 架构

```
Python(fetch_page.py) → subprocess → Node.js(sdenv_rs6.js) → sdenv
                                                                  ↓
                                                         jsdomFromUrl()
                                                                  ↓
                                                         412 挑战 → VM 执行
                                                                  ↓
Python ← stdout Cookie ← S+T Cookie对 ← cookieJar.getCookieStringSync()
   ↓
requests.get(Cookie) → HTTP 200 → data/journal_guid.html
```

## 防护识别

| 项目 | 详情 |
|------|------|
| **防护产品** | **瑞数 RS6（RuiShu Version 6）** |
| **识别依据** | `$_ts` 全局对象 + `$_ts.nsd` + `$_ts.cd` 混淆载荷 + `r='m'` 属性标记 |
| **服务器** | Tengine/3.1.0 + cqvip lib cache server |
| **状态码** | `412 Precondition Failed` — 首次请求返回 JS 挑战页 |
| **Set-Cookie** | `6HZbKHDjIEcgS=60t_...` (动态 cookie 名/值，每次变化，HttpOnly，10 年过期) |
| **Meta 签名** | `<meta id="FbkwzLN5XOx0" content="..." r='m'>` — 混淆后的签名载荷 |
| **JS 挑战** | `<script r='m'>$_ts=window['$_ts'];...$_ts.nsd=52203;$_ts.cd="qxrErrAl..."</script>` |
| **外部 JS** | 226KB VM JS（`/Qikan/Scripts/rs/xxx.js`） |

### 请求对比

```
requests.get() → 412 (3KB JS 挑战页)
curl-cffi chrome120 → 412 (3KB JS 挑战页) ← TLS 指纹不是主因
浏览器正常访问 → 200 (真实页面)
```

### 攻防流程

```
首次请求 → 412 + $_ts JS 挑战 + Set-Cookie(初始)
浏览器执行 $_ts JS → 从 meta/nsd/cd 提取数据 → 生成合法 Cookie 值
二次请求(带合法Cookie) → 200 真实页面
```

## sdenv 环境伪装方案

### 为什么选 sdenv

RS6 的 VM 代码与环境深度绑定，路径 A 算法还原不可行。实际验证通过的方案：

1. Node.js 项目安装 `sdenv`（需 Node >= 20.19.5 + VS Build Tools C++ 编译）
2. `jsdomFromUrl(url)` 自动处理 412 挑战 → VM 执行 → S+T Cookie 生成
3. `dom.cookieJar.getCookieStringSync()` 提取 Cookie
4. Cookie 传递给 Python `requests` → HTTP 200 真实页面

### 环境要求

| 依赖 | 版本/说明 |
|------|----------|
| Node.js | v20.19.5+（sdenv v1.1.3 要求 undici ^7） |
| VS 2022 Build Tools | C++ 桌面开发组件（node-gyp 编译 canvas/native 模块） |
| pnpm | 包管理 |
| .npmrc | `onlyBuiltDependencies: [canvas, sdenv]` |

### sdenv 核心流程

```javascript
const { jsdomFromUrl } = require('sdenv');

// 1. 加载目标 URL（自动处理 412）
const dom = await jsdomFromUrl(url, {
  userAgent: '...Chrome/120...',
});

// 2. 等待 VM 执行完成
await new Promise(resolve => {
  dom.window.addEventListener('sdenv:exit', resolve);
  setTimeout(resolve, 12000); // 超时保护
});

// 3. 提取 Cookie
const cookies = dom.cookieJar.getCookieStringSync(url);
```

## 分析路径对比

### ✅ 路径 B：环境伪装（已采用）

- sdenv + jsdom 自动处理 412 挑战
- 无需逆向 VM 字节码
- 稳定性高，维护成本低

### ❌ 路径 A：算法追踪（不推荐）

- RS6 的 `$_ts.cd` 载荷经过三层嵌套 VM 执行
- 提取纯净算法代价极高
- 仅当 sdenv 不可用时才考虑

## 文件清单

| 文件 | 用途 |
|------|------|
| `code/fetch_page.py` | Python 入口，subprocess 调用 Node.js + requests 请求 |
| `code/sdenv_rs6.js` | Node.js sdenv 核心，jsdomFromUrl → Cookie 生成 |
| `code/web_config/412_challenge.html` | 服务端返回的 412 JS 挑战页样本 |
| `code/web_config/cookies.txt` | 成功生成的 Cookie（供 Python 使用） |
| `code/web_config/412_headers.json` | 412 请求头对照 |
| `code/web_config/jsdom_result.json` | sdenv 输出快照 |
| `data/journal_guid.html` | 爬取结果：真实期刊导航页面 |
| `init/task_cqvip_journal_guid.md` | 任务规格书（含 RS6 识别依据和分析路径） |

## 限制条件

- **不使用** Playwright / Selenium 等浏览器自动化
- 最终方案：Node.js (sdenv) 生成 Cookie + Python (requests) 执行 HTTP 请求
- 符合 hello_js_reverse_skill 四条红线要求
